# Copy to .env and customize for local / production
# DATABASE_URL=sqlite:///./match_monitor.db
# CORS_ORIGINS=["http://localhost:3000"]

# Admin authentication — REQUIRED in production
ADMIN_PASSWORD=changeme
# Generate a random 32+ char string, e.g. `python -c "import secrets;print(secrets.token_urlsafe(32))"`
SESSION_SECRET=dev-secret-change-me

# Cookie options: set cookie_secure=true when serving over HTTPS
# COOKIE_SECURE=false
# COOKIE_SAMESITE=lax
