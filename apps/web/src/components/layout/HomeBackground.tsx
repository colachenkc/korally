"use client";

/**
 * Full-dark theme: hero background images are disabled. The body itself is
 * `bg-cream-50` (dark-flipped near-black), so this component is a no-op —
 * kept in the layout tree in case we later want to re-introduce per-route
 * decorative gradients / spotlights.
 */
export function HomeBackground() {
  return null;
}
